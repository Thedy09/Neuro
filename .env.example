# NEURO X Frontend Environment
# Copy to .env and fill in values.

# Backend API used by browser features that call FastAPI (no trailing slash).
# Dev local API : http://localhost:8000
# Prod (Render) déployée : https://neuro-bii5.onrender.com
VITE_API_URL=https://neuro-bii5.onrender.com

# Solana cluster: devnet (default) or mainnet-beta (production).
# Drives wallet network, explorer links, and UI labels.
VITE_SOLANA_CLUSTER=devnet

# Solana RPC used by the frontend (takes precedence over the public cluster RPC).
# Production: put your QuickNode mainnet HTTPS endpoint here.
VITE_SOLANA_RPC_URL=https://api.devnet.solana.com

# QuickNode reference fields for your own tracking.
# The frontend only needs VITE_SOLANA_RPC_URL.
VITE_QUICKNODE_RPC_URL=
VITE_QUICKNODE_WS_URL=

# ElevenLabs Conversational AI agent ID.
VITE_ELEVENLABS_AGENT_ID=

# Where the "Upgrade to Pro" paywall button points (pricing page, Stripe checkout...).
# Defaults to a contact mailto when empty.
VITE_UPGRADE_URL=
